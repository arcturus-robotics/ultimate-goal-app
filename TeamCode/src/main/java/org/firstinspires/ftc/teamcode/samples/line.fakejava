ArcturusDrive drive = new ArcturusDrive(hardwareMap);
Trajectory traj = drive.trajectoryBuilder(new Pose2d())
                .lineTo(new Vector2d(5.0, 5.0)); or something idk
                .build();
drive.followTrajectory(traj);
